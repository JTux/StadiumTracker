@using StadiumTracker.WebMVC.Helpers

@{
    var name = Html.CurrentViewName();
    var controller = Html.ControllerName();
    if (controller == "TeamController")
    {
        <p>
            @Html.ActionLink("View Leagues", "../League")
        </p>
    }
}